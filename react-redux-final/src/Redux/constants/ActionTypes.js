// We define constants here to be used instead of typing strings in the other moving parts of Redux. This is so that any typos in the names of the constants will throw an error (undefined constant) rather than having the app silently fail in the background.
export const ADD_SEARCH_RESULTS = 'ADD_SEARCH_RESULTS';
